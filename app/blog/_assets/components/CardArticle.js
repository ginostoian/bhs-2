import Link from "next/link";
import Image from "next/image";
export default function CardArticle({article,tag="h2",showCategory=true,isImagePriority=false}){const Heading=tag;const src=article.image?.src||article.image?.urlRelative;return <article className="bh-project-card"><Link href={`/blog/${article.slug}`} rel="bookmark">{src ? <Image src={src} alt={article.image.alt||article.title} width={800} height={600} priority={isImagePriority} sizes="(max-width:700px) 100vw, 50vw"/> : null}{showCategory ? <p className="bh-eyebrow" style={{marginTop:20}}>{article.categories.map(c=>c.titleShort||c.title).join(" · ")}</p> : null}<Heading style={{fontSize:28,lineHeight:1.2,fontWeight:500,marginTop:12}}>{article.title}</Heading><p>{article.description}</p><p className="bh-text-link" style={{marginTop:16}}>Read the guide →</p></Link></article>;}
